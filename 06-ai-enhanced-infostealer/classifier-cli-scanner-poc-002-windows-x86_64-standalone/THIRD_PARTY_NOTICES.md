# Third-party notices

This standalone scanner POC bundle contains CPU-only ONNX Runtime code built
from source for this package and selected by the locked Rust `ort` integration
(ONNX Runtime 1.28 at build time). ONNX Runtime is MIT licensed. The `ort` and
`ort-sys` Rust crates are MIT OR Apache-2.0.

This is a presentation POC, not a final release bundle. Phase 6 must produce a
complete third-party license inventory and distribution/source record before a
production release.
